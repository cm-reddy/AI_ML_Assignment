# prometheus.yml
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "iris_api"
    static_configs:
      - targets: ["host.docker.internal:8000"]  # For local Docker

  # - job_name: "housing_api"
  #   static_configs:
  #     - targets: ["host.docker.internal:8001"]  # If housing API runs on port 8001
